# How to

To update dependencies (remember to commit changes to Gemfile.lock)

```
make update
# or
bundler update
```

To run local server:

```
bundle exec jekyll serve --port 4000
# or
make run
```

# TODO

Fix code snippets
